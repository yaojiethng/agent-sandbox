# Agent Handover

**Session date:** 2026-03-14
**Milestone:** M2.1 — General Capability Layer Prototype
**Session type:** Documentation + Investigation

## Completed this session

| File | Change |
|---|---|
| `docs/concepts/two_layer_model.md` | Full rewrite — two-layer model revised: capability layer container always present, MCP server optional, three configurations defined, `sandbox/` as shared volume mount (not `/working`), deferred content removed |
| `docs/architecture/execution_model.md` | Full rewrite — two-container model: capability layer owns `sandbox/` + diff pipeline, reasoning layer mounts `sandbox/` as shared volume, container lifecycle section added, entrypoint sequences split |
| `docs/development/roadmap.md` | M2 Upcoming Milestones section replaced: sub-milestones reordered (M2.1 general → M2.2 modularisation → M2.3 apply → M2.4 session → M2.5 vault), task lists rewritten to reflect agreed model |
| `docs/development/roadmap_future.md` | `## Deferred (Unplanned)` section added — live mount as deferred UX item |
| `docs/operations/task_policy.md` | Handover Next task rule updated — points to full milestone section; template updated |
| `docs/operations/documentation_policy.md` | Editing Guidelines — policy check before drafting rule added |

## Next task

**M2.1 — General Capability Layer Prototype** (`## Upcoming Milestones → #### M2.1` in `roadmap.md`)

Read the full M2.1 section in `roadmap.md` before starting. Key context:

- `docs/concepts/two_layer_model.md` — agreed model; read before touching any implementation
- `docs/architecture/execution_model.md` — target two-container design; code must conform to this
- The two-container model is fully specced. Implementation begins directly — no open decisions remain for M2.1.

Files operator should provide at session start:
- `start_agent.sh` (current, at `providers/opencode/start_agent.sh`)
- `container-entrypoint.sh` (current, at `providers/opencode/container-entrypoint.sh`)
- `Dockerfile` (current, at `providers/opencode/Dockerfile`)
- `security.md` and `agent_workflow.md` (for doc updates required by M2.1)

Grep to run at session start:
```bash
grep -n "^##\|^###" docs/architecture/execution_model.md
grep -n "^##\|^###" docs/concepts/two_layer_model.md
```

## Watch out for

- `container-entrypoint.sh` currently lives at `providers/opencode/` — M2.1 splits it into capability layer and reasoning layer entrypoints; move to `libs/` is M2.2, not now
- `sandbox/` naming is now consistent across both containers (shared Docker volume); do not reintroduce `/working` or any other mount path alias
- M2.1 doc tasks (`security.md`, `agent_workflow.md`) are part of the milestone and must be completed before M2.1 is marked done
