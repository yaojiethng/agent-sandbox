# Agent Handover

**Session date:** 2026-03-13
**Milestone:** M1.5 — Workflow Convergence & Directory Restructuring (complete); M2 promoted
**Session type:** Implementation + Documentation + Housekeeping

---

## Completed this session

| File | Change |
|---|---|
| `providers/opencode/start_agent.sh` | `--root` → `--project`; `SANDBOX_DIR` derived by convention or `--sandbox` override; `.bootstrap` → `.agent-input`; `AGENT_INPUT_DIR_NAME` / `AGENT_WORKSPACE_DIR_NAME` defined once and passed to container via `-e`; brief and env resolve relative to `SANDBOX_DIR`; operator `input/` created in `.agent-input/` |
| `scripts/agent-sandbox.sh` | `--root` → `--project`; `--sandbox` added; `apply` requires both `--project` and `--sandbox`; `rebuild` extracts `--project` |
| `scripts/apply_workspace.sh` | `--root` split into `--project` (git ops) and `--sandbox` (diff location) |
| `providers/opencode/container-entrypoint.sh` | All paths derived from `$AGENT_INPUT_DIR_NAME` / `$AGENT_WORKSPACE_DIR_NAME` env vars; operator `input/` copied into `sandbox/` after snapshot |
| `scripts/dry_run.sh` | Uses same env vars; reports on `input/` directory |
| `libs/_templates/Makefile.template` | `PROJECT_ROOT` → `PROJECT_DIR` + `SANDBOX_DIR` explicit; all targets updated; `rebuild` `--serve` bug removed |
| `docs/architecture/execution_model.md` | New directory layout, terminology table, mount shape, input channel lifecycle, `.agent-input/` lifecycle, directory name env var pattern |
| `docs/concepts/agent_workflow.md` | New host directory structure, mount rules table, updated operator workflow, input channel lifecycle section |
| `docs/development/roadmap.md` | M1.5 complete in summary table; M1.5 section removed; Deferred Decisions removed; M2 promoted into Upcoming Milestones; M2 summary table row updated to local anchor |
| `docs/development/roadmap_future.md` | M2 section removed (promoted); checkpoint tooling added to M2.3; checkpoint decisions added to M2.4 |
| `docs/development/changelog.md` | M1.5 entry appended (see `changelog_m1.5.md`) |
| `docs/operations/roadmap_policy.md` | Milestone Promotion section added; scope line updated to include `roadmap_future.md`; Trigger A step 6 added |
| `sandbox-onboarding_skill.md` | Updated for new directory layout; modularised with comment-marked variable definitions; verification checklist updated |

---

## Next task

**M2.1 — Capability Layer Prototype: Vault. Start with the two open decisions.**

Both must be resolved before any implementation begins (per M2.1 task list in `roadmap.md`):
1. Working mount strategy — live vault mount vs sandbox copy in MCP server container
2. MCP server selection — evaluate candidates against criteria in `investigation_mcp_server.md`

Run at session start:
```bash
grep -n "^##" docs/discussions/investigation_mcp_server.md
grep -n "^##" docs/concepts/two_layer_model.md
```

Read the Conclusion section of `investigation_mcp_server.md` and the MCP server candidates table before proposing a recommendation on either decision.

---

## Watch out for

- `container-entrypoint.sh` is still at `providers/opencode/` — move to `libs/` is scoped to M2.3, not now.
- The `rebuild` target in `Makefile.template` previously had `--serve` hardcoded (pre-existing bug); it has been removed. Flag to operator in case it was intentional for any existing onboarded project.
- `roadmap_policy.md` has now been updated to document the `roadmap_future.md` split and promotion rule — watch-out from previous handover is resolved.
