---
# hadolint configuration
ignored:
  - DL3008   # Pin versions in apt-get install — not project convention
  - DL3016   # Pin versions in npm install — not project convention
  - DL3047   # wget without progress bar
  - DL3059   # Multiple consecutive RUN instructions
  - DL4006   # Pipefail — handled per-file where needed
